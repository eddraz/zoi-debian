# Written by zoi-theme. Lemurs substitutes $name from this file into config.toml.
background = "{{ background }}"
foreground = "{{ foreground }}"
accent = "{{ accent }}"
muted = "{{ muted }}"
red = "{{ red }}"
