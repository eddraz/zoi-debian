#%PAM-1.0
#
# Debian display-manager stack for Lemurs.
# Do not `include login`: /etc/pam.d/login has `session required pam_loginuid.so`.
# Lemurs runs as a systemd service, so writing /proc/self/loginuid returns EPERM
# (coastalwhite/lemurs#166). Lemurs then shows "authentication failed" after a
# valid password. Keep pam_loginuid optional.

auth      requisite pam_nologin.so

@include common-auth
@include common-account
@include common-password

session   required        pam_env.so readenv=1
session   required        pam_env.so readenv=1 envfile=/etc/default/locale
session   required        pam_limits.so
session   optional        pam_keyinit.so force revoke
session   optional        pam_loginuid.so
session   [success=ok ignore=ignore module_unknown=ignore default=bad] pam_selinux.so close
@include common-session
session   [success=ok ignore=ignore module_unknown=ignore default=bad] pam_selinux.so open
